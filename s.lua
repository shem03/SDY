
LOG("------------------------------")

LOG("------------------------------")


LOG("------------------------------")

local skynet = require "skynet"

-- g.users[101746].r_password = nil
-- g.users[101746].roomid = nil

-- g.users[101790].r_password = nil
-- g.users[101790].roomid = nil

-- g.users[101792].r_password = nil
-- g.users[101792].roomid = nil




LOG(tostring(g.users[101790]))
LOG(tostring(g.users[101792]))
LOG(tostring(g.users[101746]))
-- local room = rooms.all[101838]
-- room.members = {}
-- room.agent = nil
-- room.owner = nil
-- room.roundid = nil
-- if room.password then
-- 	g.passwords[room.password] = nil
-- 	room.password = nil
-- end

-- LOG(tostring(rooms.all[101838]))

-- skynet.kill(1818)

